<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Action désactivée - utilise l'ancien API non compatible Odoo 18 -->
    <!-- 
    <record id="saas_action_run_ir_action_todo" model="ir.actions.server">
            <field name="name">SaaS Run Remaining Action Todo</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="base.model_res_config" />
            <field name="state">code</field>
            <field name="code">
        action = {}
        if uid and env['res.users'].browse(uid).has_group('base.group_system'):
        todos = self._next_action(cr, uid) or {}
        config = todos and todos.action_launch(context=context)
        if config.get('type') not in ('ir.actions.act_window_close',):
        action = config
        action = action or env.ref('base.open_menu').action_launch(context=context)
            </field>
        </record>
    -->
    <record id="action_saas_dashboard" model="ir.actions.client">
        <field name="name">SaaS Dashboard</field>
        <field name="tag">saas_client.dashboard</field>
    </record>
</odoo>
